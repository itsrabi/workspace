{
  "name": "Fedora Dev Container Base (Neovim + rg + fd)",

  // Prefer the prebuilt image from your ACR. Placeholder values only.
  // Uncomment and update when you publish the image.
  // "image": "<ACR_LOGIN_SERVER>/<IMAGE_NAME>:latest",

  // Or build locally from this repo.
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "..",
    "args": {
      "USERNAME": "developer",
      "USER_UID": "1000",
      "USER_GID": "1000",

      // Placeholder: set to your personal dotfiles repo
      "DOTFILES_REPO": "",
      "DOTFILES_REV": "master"
    }
  },

  "remoteUser": "developer",
  // Forward common dev server ports to the Windows host.
  // Ranges are forwarded to the same port number on the host.
  "forwardPorts": [
    "3000-3999",
    "5000-5999",
    "8000-8999"
  ],
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash",
    "terminal.integrated.fontFamily": "Cascadia Code NF"
  }
}
